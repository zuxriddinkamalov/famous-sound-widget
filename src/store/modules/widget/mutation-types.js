/* ============
 * Mutation types for the widget module
 * ============
 *
 * The mutation types that are available
 * on the widget module.
 */

export const SET_STATE = 'SET_STATE';
export const RESET_STATE = 'RESET_STATE';

export default {
  SET_STATE,
  RESET_STATE,
};
